apiVersion: v1
kind: Service
metadata:
    name: "akspersonicledev-9b54"
    labels:
        app: "akspersonicledev-9b54"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "akspersonicledev-9b54"